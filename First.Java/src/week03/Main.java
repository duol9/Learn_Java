package week03;

public class Main {
    // Car.java 클래스 이용해서 객체를 만듦
    // Car.java 클래스를 Public으로 정의했기 때문에 사용할 수 있음
    public static void main(String[] args) {
        /*Car.java car1 = new Car.java();   // new 키워드로 생성자 호출
        Car.java car2 = new Car.java();   // 새로운 객체를 만들기 위한 명령어
        Car.java car3 = new Car.java();
        System.out.println(car1);   //객체를 가져다 별도 공간에 주소를 부여

        Car.java[] carArray = new Car.java[3];
        car1.changeGear('P'); // 필드 변경 가능
        carArray[0] = car1;

        car2.changeGear('N');
        carArray[1] = car2;

        car3.changeGear('D');
        carArray[2] = car3;

        for (Car.java car : carArray) {
            System.out.println("car.gear = " + car.gear);
        } */

        // 필드 관련
        /*Car.java car = new Car.java();
        // 초기값 기본값 확인 : 초기값을 준 것은 그 값이 들어가고, 아닌 값은 default value가 set
        System.out.println("car.model = " + car.model);
        System.out.println("car.color = " + car.color);
        System.out.println();

        System.out.println("car.speed = " + car.speed);
        System.out.println("car.gear = " + car.gear);
        System.out.println("car.lights = " + car.lights);
        System.out.println();

        System.out.println("car.tire = " + car.tire);
        System.out.println("car.door = " + car.door);
        System.out.println();

        // 필드 사용
        car.color = "blue";
        car.speed = 100;
        car.lights = false;

        System.out.println("car.color = " + car.color);
        System.out.println("car.speed = " + car.speed);
        System.out.println("car.lights = " + car.lights);*/
    }
}
